@model IEnumerable<fragrance.Models.contact>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.info_contact)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.info_contact)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_contact }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_contact }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_contact })
        </td>
    </tr>
}

</table>
