
@model IEnumerable<fragrance.Models.product_type>

@foreach (var item in Model)
{
    <li>
        <a href="@Url.Action("TypeProduct","Fragrance", new { id = item.id_prt})">@item.name_prt</a>
    </li>
}
